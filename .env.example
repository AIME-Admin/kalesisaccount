# Copy to .env and fill in before final deployment.

# Final public site URL (used for canonical, Open Graph, JSON-LD).
VITE_SITE_URL=https://kalesis-accounting.gr/

# Google Form (intake). Leave empty to show the placeholder card.
# Embed URL: the form's "Send > <> Embed HTML" src (…/viewform?embedded=true)
VITE_GOOGLE_FORM_EMBED_URL=
# Public URL: the normal shareable form link (opens in a new tab)
VITE_GOOGLE_FORM_PUBLIC_URL=

# Optional analytics (not wired by default — add tags when ready)
VITE_GA_ID=
VITE_GTM_ID=
